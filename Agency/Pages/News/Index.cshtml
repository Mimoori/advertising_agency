@page
@model Agency.Pages.News.IndexModel

@{
    ViewData["Title"] = "Index";
}



<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Article[0].title)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Article[0].descriptions)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Article[0].datetime)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Article) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.title)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.descriptions)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.datetime)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.id_news">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.id_news">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.id_news">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

<a href="../Index">ToMain</a>
